apiVersion: v1
kind: Service
metadata:
  name: cloudflared-metrics
  namespace: routing
  labels:
    app: cloudflared
spec:
  type: ClusterIP
  selector:
    app: cloudflared
  ports:
    - name: metrics
      port: 2000
      targetPort: 2000
