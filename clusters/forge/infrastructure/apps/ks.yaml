apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-blog
  namespace: flux-system
spec:
  interval: 10m
  dependsOn:
    - name: cert-manager-config
    - name: routing-traefik
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./infrastructure/apps/blog
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
